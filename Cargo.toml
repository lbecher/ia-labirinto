[package]
name = "ia-labirinto"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.12.0", features = ["dynamic_linking"] }
pyo3 = { version = "0.20.0" }
simple-matrix = { version = "0.1.2" }
